/**
 * Persistent layout state for diagram nodes, links, docked panels, and the workspace. These classes describe how the model is shown, not what the model means.
 */
package lu.kbra.modelizer_next.layout;
